var skip = 2;
        function printRange()
        {
          for (var start = 0; start < 8; start=start+2 ){
          start= start + skip;
          console.log(start);
          }
          
          }
         
        printRange();